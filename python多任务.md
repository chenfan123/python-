# 多任务 多进程 多线程

## 多任务

同时执行能够充分利用 cpu 资源，提高程序的执行效率。

- 并发 ： 在一段时间内，交替执行任务
- 并行 ： 在一段时间内，真正的同时一起执行多个任务，必须要多核

### 进程

是 CPU **资源分配**的最小单位，是**操作系统进行资源分配和调度运行的基本单位**。

创建子进程会对主进程资源进行拷贝，所以子进程和主进程的资源是隔离的。

##### 进程的创建

multiprocessing 模块
`子进程对象 = multiprocessing.Process(group=None,target=函数名,name=None,args=(传递的参数1,传递的参数2),kwargs={})`
group: 进程组，目前只能使用 None，一般不使用
target: 要执行的任务
args: 要传递的参数，元祖类型
kwargs: 要传递的参数，字典类型
name: 进程名称，一般不使用
daemon: 是否守护进程，一般不使用

##### 进程的启动

`子进程对象.start()`

> 进程的注意事项：
> 进程之间数据是相互隔离的，因为子进程相当于是父进程的副本，会将父进程的 main 外资源拷贝一份
> 主进程会等所有的子进程执行结束再结束

##### 不让主进程等待子进程

###### 方式一：子进程设置守护进程

目的：是主进程推出子进程销毁，不让主进程再等待子进程去执行。
`子进程对象.daemon = True`

###### 方式二：子进程自动终止子进程

`子进程对象.start()`
`子进程对象.terminate()`
不建议，可能会导致变成僵尸进程，交由 python 解释器自动回收。

### 线程

线程用来实现多任务编程，是 cpu **调度**的基本单位，每个进程至少都有一个线程，而这个线程就是主线程，主线程可以创建子线程。

#### 线程的创建
