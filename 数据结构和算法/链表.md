# 链表

顺序表存储时需要连续的内存空间，要扩充顺序表时会出现内存不充足，完成不了扩展扩充。

链表就不需要连续的存储空间，每个节点有 2 部分：元素域，下一个节点的内存地址。最后一个节点链接域为 None

表元素域 item 用来存放具体的数据
链接域 next 用来存放下一个节点的位置
变量 head 指向链表的头节点的位置，从 head 出发能找到表中的任意节点

种类：

1. 单向链表
2. 双向链表
3. 循环链表
4. 双向循环链表
