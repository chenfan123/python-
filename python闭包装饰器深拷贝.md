# 闭包装饰器和深拷贝

## 闭包

作用：保存函数内的变量，而不会随着调用完函数而被销毁。

什么是闭包：**使用外部函数变量**的**内部函数**称为闭包

构成条件：

1. 有嵌套；（在函数嵌套的前提下）
2. 有引用：内部函数使用了外部函数的变量（还包括外部函数的参数）
3. 有返回：外部函数返回了内部函数名

```python
# 闭包
def test():
    a = 100

    def test_in(b):
        print(a + b)
    return test_in


f = test()
f(6)
```
